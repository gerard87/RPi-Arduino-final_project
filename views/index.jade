extends layout

block content
    h1= title
    p Welcome to #{title}

    script.
        $(function () {
            $('#btn').on('click', function (event) {

                console.log("Call /readvalues post");

                $.ajax({
                    type: "POST",
                    url: '/readvalues',
                    data: {
                    }
                });

            });

        });

    a#btn.mdl-button.mdl-js-button.mdl-button--raised.mdl-js-ripple-effect.mdl-button--accent.mdl-color--blue-grey-800.mdl-color-text--white Read values
